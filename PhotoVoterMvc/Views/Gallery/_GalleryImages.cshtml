@model System.Collections.Generic.IEnumerable<PhotoVoterMvc.Services.Model.GalleryData>
   <ul id="photos" class="photos">
      @foreach (var item in Model) { 
        Html.RenderPartial("_GalleryImage", item);
      }  
   </ul>